export interface Config {
    id: string;
    name: string;
    imageUrl: string;  // Add this line (Make this a requirement or gay)
    description: string;
    version: string;
    author: string;
    downloadUrl: string;
    createdAt: string;
    updatedAt: string;
  }